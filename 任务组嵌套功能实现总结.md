# 任务组嵌套功能实现总结

## 功能概述

本次重写实现了以下核心功能：

1. **展示所有节点（包括任务组节点）**：后端 `getJobCompose` 方法现在能够递归返回所有节点，包括嵌套的任务组节点。

2. **任务组节点支持展开/缩放**：
   - 任务组节点可以包含子节点（包括其他任务组节点）
   - 展开时会包围所有子节点
   - 当子任务组下有子任务组节点时，两个都展开，任务组的边框会自动变大

3. **保存任务组时记录完整信息**：
   - 保存子节点的位置信息（nodePositionX, nodePositionY）
   - 保存子节点的入度（nodeInDegree）和出度（nodeOutDegree）
   - 子节点信息除了 id 和 jobId 不一样，位置不一样，其他基本一样
   - 位置信息与原位置对应

## 修改的文件

### 1. 后端文件

#### `/workspace/cc-job/cc-job-admin/src/main/java/com/cc/job/admin/task/service/impl/JobComposeServiceImpl.java`

**主要修改：**

- **`buildAllNodesAndEdgesRecursive` 方法（第 640-835 行）**：
  - 递归构建所有节点和边（包括嵌套的任务组）
  - 为每个任务组节点设置正确的 `children` 属性（包含所有直接子节点的ID列表）
  - 计算任务组的边界（width、height）以包围所有子节点
  - 使用数据库中保存的位置信息，或计算中心位置
  - 递归处理嵌套的任务组节点，将所有子节点和边添加到总列表

- **`getJobCompose` 方法（第 566-638 行）**：
  - 调用 `buildAllNodesAndEdgesRecursive` 递归构建所有节点和边
  - 返回所有节点（包括嵌套的任务组节点）和所有边（包括嵌套任务组内的边）
  - 创建根任务组节点（用于前端展示）
  - 根据 type 参数决定是否调整节点位置（type=1 为运行时模式）

- **`operateToSaveJobCompose` 方法（第 119-268 行）**：
  - 批量保存 JobInfo 和 JobNode
  - 递归保存嵌套的任务组节点
  - 为每个节点设置位置信息（nodePositionX, nodePositionY）
  - 计算并保存节点的入度（nodeInDegree）和出度（nodeOutDegree）
  - 确保任务组节点有正确的 width 和 height 属性

- **`operateToUpdateJobCompose` 方法（第 312-563 行）**：
  - 更新现有节点或创建新节点
  - 处理嵌套的任务组节点
  - 更新节点的位置信息
  - 重新计算并更新节点的入度和出度
  - 删除不再存在的节点

**关键改进：**

1. **支持嵌套结构**：
   ```java
   // 递归构建子任务组的节点和边
   buildAllNodesAndEdgesRecursive(node.getJobId(), childNodeVos, childEdgeVos, randomId, jobId);
   
   // 将所有子节点和边添加到总列表（前端需要这些信息来展示嵌套结构）
   allNodeVos.addAll(childNodeVos);
   allEdgeVos.addAll(childEdgeVos);
   ```

2. **收集直接子节点ID**：
   ```java
   // 收集所有直接子节点的ID（用于前端识别任务组的children）
   List<JobNode> directChildNodes = jobNodeService.list(
       new LambdaQueryWrapper<JobNode>().eq(JobNode::getJobParentId, node.getJobId())
   );
   List<String> directChildIds = new ArrayList<>();
   for (JobNode childNode : directChildNodes) {
       directChildIds.add(randomId + childNode.getId());
   }
   ```

3. **计算任务组边界**：
   ```java
   double[] styleArr = getMaxWidthHeight(childNodeVos);
   double groupWidth = styleArr[1] - styleArr[3] + 40;  // 增加padding
   double groupHeight = styleArr[2] - styleArr[0] + 40;  // 增加padding
   
   propertiesMap.put("width", groupWidth);
   propertiesMap.put("height", groupHeight);
   propertiesMap.put("children", directChildIds);  // 直接使用List，不转JSON字符串
   ```

4. **保存子节点位置和入度/出度**：
   ```java
   // 设置节点位置
   jobNode.setNodePositionX(node.x);
   jobNode.setNodePositionY(node.y);
   
   // 批量设置节点的入度和出度
   for (JobNode item : nodeFromDbList) {
       item.setNodeInDegree(inDegreeMap.getOrDefault(item.getId(), 0L));
       item.setNodeOutDegree(outDegreeMap.getOrDefault(item.getId(), 0L));
   }
   
   jobNodeService.updateBatchById(nodeFromDbList);
   ```

### 2. GUI 后端文件（桌面端）

#### `/workspace/cc-job/cc-job-gui/src/main/java/com/cc/job/gui/model/JobComposeData.java`

**主要修改：**

- **`NodeData` 类（第 46-152 行）**：
  - 添加了 `childrenNodes` 字段：用于存储子节点列表（支持嵌套展示）
  - 添加了 `jobParentId` 字段：用于存储父任务组ID
  - 更新了 getter 和 setter 方法
  - 更新了 `toString` 方法，显示子节点数量

**关键改进：**

```java
private List<NodeData> childrenNodes;  // 子节点列表（用于任务组节点的嵌套展示）
private Long jobParentId;  // 父任务组ID

public List<NodeData> getChildrenNodes() {
    return childrenNodes;
}

public void setChildrenNodes(List<NodeData> childrenNodes) {
    this.childrenNodes = childrenNodes;
}

public Long getJobParentId() {
    return jobParentId;
}

public void setJobParentId(Long jobParentId) {
    this.jobParentId = jobParentId;
}
```

#### `/workspace/cc-job/cc-job-gui/src/main/java/com/cc/job/gui/service/JobPartService.java`

**主要修改：**

- **`parseJobComposeData` 方法（第 173-388 行）**：
  - 添加了对 `jobParentId` 字段的解析
  - 添加了对 `childrenNodes` 字段的解析
  - 递归解析嵌套的子节点列表

- **新增 `parseNodeData` 方法（第 390-504 行）**：
  - 递归解析节点数据（用于解析嵌套的任务组节点）
  - 解析节点的所有属性（nodeType, jobName, jobId, jobParentId, triggerStatus, coordinates, properties）
  - 递归解析子节点列表 `childrenNodes`

**关键改进：**

1. **解析 jobParentId**：
   ```java
   // 解析 jobParentId 字段
   if (nodeMap.get("jobParentId") != null) {
       try {
           node.setJobParentId(((Number) nodeMap.get("jobParentId")).longValue());
       } catch (Exception e) {
           logger.error("解析节点 jobParentId 失败: {}", e.getMessage(), e);
       }
   }
   ```

2. **递归解析嵌套子节点**：
   ```java
   // 递归解析子节点列表
   Object childrenNodesObj = nodeMap.get("childrenNodes");
   if (childrenNodesObj instanceof List) {
       List<JobComposeData.NodeData> childrenNodesList = new ArrayList<>();
       List<?> childrenNodesList0 = (List<?>) childrenNodesObj;
       for (Object childNodeObj : childrenNodesList0) {
           if (childNodeObj instanceof Map) {
               Map<?, ?> childNodeMap = (Map<?, ?>) childNodeObj;
               JobComposeData.NodeData childNode = parseNodeData(childNodeMap);  // 递归调用
               if (childNode != null) {
                   childrenNodesList.add(childNode);
               }
           }
       }
       node.setChildrenNodes(childrenNodesList);
   }
   ```

3. **递归解析方法**：
   ```java
   /**
    * 递归解析节点数据（用于解析嵌套的任务组节点）
    */
   private JobComposeData.NodeData parseNodeData(Map<?, ?> nodeMap) {
       JobComposeData.NodeData node = new JobComposeData.NodeData();
       
       // 解析所有节点属性...
       // 递归解析子节点列表...
       
       return node;
   }
   ```

### 3. GUI 前端文件（桌面端）

#### `/workspace/cc-job/cc-job-gui/src/main/java/com/cc/job/gui/view/NodeCanvas.java`

**已有功能（无需修改）：**

- **`loadFromComposeData` 方法（第 1109-1334 行）**：
  - 加载节点和边
  - 识别任务组节点（CustomGroup 或 custom-group）
  - 延迟处理任务组节点（先处理普通节点，再处理任务组节点）
  - 调用 `createGroupContainerRecursive` 递归创建任务组容器

- **`createGroupContainerRecursive` 方法（第 1373-约1550 行）**：
  - 递归创建任务组容器（支持嵌套结构）
  - 解析子节点ID列表
  - 先处理嵌套的任务组节点
  - 创建任务组容器（GroupContainer）
  - 设置容器的位置和大小

**已实现的关键功能：**

1. **识别任务组节点**：
   ```java
   boolean isGroupNode = false;
   if (nodeType != null && !nodeType.trim().isEmpty() && !"null".equals(nodeType)) {
       String normalizedType = nodeType.trim();
       isGroupNode = normalizedType.equals("CustomGroup") || 
                    normalizedType.equalsIgnoreCase("custom-group");
   }
   
   if (isGroupNode) {
       // 任务组节点延迟处理
       groupNodeDataList.add(nodeData);
       continue;
   }
   ```

2. **递归创建任务组容器**：
   ```java
   // 递归处理任务组节点（从最外层到最内层）
   for (JobComposeData.NodeData groupNodeData : groupNodeDataList) {
       try {
           createGroupContainerRecursive(groupNodeData, composeData, nodeMap, 
                                        containerMap, groupNodeMap);
       } catch (Exception e) {
           logger.error("创建任务组容器失败: {}", e.getMessage(), e);
       }
   }
   ```

3. **嵌套任务组处理**：
   ```java
   // 先处理嵌套的任务组节点
   for (String childId : childNodeIds) {
       JobComposeData.NodeData childGroupData = groupNodeMap.get(childId);
       if (childGroupData != null) {
           // 递归创建嵌套的任务组容器
           createGroupContainerRecursive(childGroupData, composeData, nodeMap, 
                                        containerMap, groupNodeMap);
           // 获取已创建的子容器
           GroupContainer childContainer = containerMap.get(childId);
           if (childContainer != null) {
               childContainers.add(childContainer);
           }
       }
   }
   ```

## 数据流程

### 1. 后端返回数据结构

```json
{
  "jobNode": {
    "id": "CustomGroup",
    "jobId": 123,
    "nodeType": "CustomGroup",
    "jobName": "根任务组",
    "nodePositionX": 400.0,
    "nodePositionY": 300.0,
    "properties": {
      "jobId": "123",
      "width": 640.0,
      "height": 440.0,
      "children": []
    }
  },
  "nodes": [
    {
      "id": "1",
      "jobId": 456,
      "nodeType": "Bean",
      "jobName": "任务节点1",
      "nodePositionX": 100.0,
      "nodePositionY": 100.0,
      "jobParentId": 123,
      "nodeInDegree": 0,
      "nodeOutDegree": 1,
      "properties": {
        "jobId": "456",
        "width": 160.0,
        "height": 90.0
      }
    },
    {
      "id": "2",
      "jobId": 789,
      "nodeType": "CustomGroup",
      "jobName": "子任务组",
      "nodePositionX": 300.0,
      "nodePositionY": 100.0,
      "jobParentId": 123,
      "nodeInDegree": 1,
      "nodeOutDegree": 0,
      "childrenNodes": [
        {
          "id": "3",
          "jobId": 111,
          "nodeType": "API",
          "jobName": "API节点",
          "nodePositionX": 50.0,
          "nodePositionY": 50.0,
          "jobParentId": 789,
          "nodeInDegree": 0,
          "nodeOutDegree": 0,
          "properties": {
            "jobId": "111",
            "width": 160.0,
            "height": 90.0
          }
        }
      ],
      "properties": {
        "jobId": "789",
        "width": 250.0,
        "height": 180.0,
        "children": ["3"]
      }
    }
  ],
  "edges": [
    {
      "id": "e1",
      "fromNodeId": "1",
      "endNodeId": "2",
      "startPoint": "{\"x\":160,\"y\":45}",
      "endPoint": "{\"x\":0,\"y\":45}",
      "properties": "{}"
    }
  ]
}
```

### 2. 前端处理流程

1. **GUI 客户端调用 `jobPartService.getJobCompose(jobId)`**
2. **JobPartService 解析 API 响应**：
   - 解析 `jobNode`（根任务组节点）
   - 解析 `nodes`（所有节点，包括嵌套的任务组节点）
   - 递归解析每个节点的 `childrenNodes`
   - 解析 `edges`（所有边）
3. **MainView 调用 `canvas.loadFromComposeData(composeData)`**
4. **NodeCanvas 渲染节点**：
   - 先创建所有普通节点
   - 延迟处理任务组节点
   - 递归创建任务组容器（GroupContainer）
   - 将子节点和子容器添加到父容器中
   - 设置容器的位置和大小

### 3. 保存流程

1. **用户点击保存按钮**
2. **MainView 收集画布数据**：
   - 收集所有节点（包括任务组节点）
   - 收集所有连接线
   - 记录每个节点的位置信息
3. **构建保存数据**：
   - 为每个节点构建 `nodeData`（包含位置、类型、properties）
   - 为任务组节点添加 `children` 列表
   - 为每条连接线构建 `edgeData`
4. **调用后端 API**：
   - `saveJobCompose` 或 `updateJobCompose`
5. **后端处理**：
   - 解析节点和边数据
   - 递归保存任务组节点和子节点
   - 计算并保存每个节点的入度和出度
   - 保存所有连接线

## 测试验证

### 功能测试要点

1. **创建嵌套任务组**：
   - 创建根任务组
   - 在根任务组中添加普通节点
   - 在根任务组中添加子任务组
   - 在子任务组中添加节点
   - 保存并重新加载，验证所有节点和边都正确显示

2. **展开/缩放**：
   - 验证任务组边框会自动包围所有子节点
   - 移动子节点时，任务组边框应自动调整
   - 嵌套任务组展开时，父任务组的边框应包含子任务组

3. **位置信息保存**：
   - 移动节点后保存
   - 重新加载，验证节点位置与保存前一致
   - 验证任务组内节点的相对位置正确

4. **入度/出度**：
   - 创建节点之间的连接
   - 保存后验证每个节点的入度和出度是否正确
   - 删除连接后，验证入度和出度是否更新

## 注意事项

1. **节点类型统一**：
   - 任务组节点类型统一为 "CustomGroup"（大写）
   - 前端可能使用 "custom-group"（小写），需要规范化处理

2. **children 字段格式**：
   - 后端返回时直接使用 List 格式
   - 前端解析时支持 JSON 字符串和 List 两种格式

3. **性能优化**：
   - 批量查询 JobInfo 和 JobNode，避免 N+1 查询问题
   - 使用 Map 预先构建映射，避免重复遍历（O(n²) -> O(n)）

4. **错误处理**：
   - 节点 jobId 为 null 时跳过，不中断整个流程
   - 解析失败时记录日志，继续处理其他节点

## 总结

本次重写成功实现了任务组的嵌套展示功能，包括：

✅ 后端能够递归返回所有节点（包括嵌套的任务组节点）
✅ 前端能够正确解析和展示嵌套结构
✅ 任务组节点支持展开/缩放，边框自动调整
✅ 保存时记录完整的节点信息（位置、入度、出度）
✅ 支持多层嵌套的任务组结构

所有功能已在代码中实现，可以进行测试验证。
