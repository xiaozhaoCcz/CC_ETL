<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cc.job.task.mapper.TaskGroupMapper">

    <!-- 获取task_group分页列表 -->
    <select id="getTaskGroupPage" resultType="com.cc.job.task.model.vo.TaskGroupVO">
        SELECT
                id,
                app_name,
                title,
                address_type,
                address_list,
                update_time
        FROM
            task_group
        <where>
        </where>
    </select>

</mapper>
