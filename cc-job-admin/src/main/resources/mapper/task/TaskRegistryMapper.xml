<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cc.job.task.mapper.TaskRegistryMapper">

    <sql id="Base_Column_List">
        t.id,
		t.registry_group,
		t.registry_key,
		t.registry_value,
        t.create_time,
		t.update_time
    </sql>

    <!-- 获取执行器分页列表 -->
    <select id="getTaskRegistryPage" resultType="com.cc.job.task.model.vo.TaskRegistryVO">
        SELECT
                id,
                registry_group,
                registry_key,
                registry_value,
                create_time,
                update_time
        FROM
            task_registry
        <where>
        </where>
    </select>

    <select id="findDead" resultType="java.lang.Long">
        SELECT t.id
        FROM task_registry AS t
        WHERE t.update_time <![CDATA[ < ]]> DATE_ADD(#{nowTime},INTERVAL -#{timeout} SECOND)
    </select>

    <select id="findAll" resultType="com.cc.job.task.model.entity.TaskRegistry">
        SELECT <include refid="Base_Column_List" />
        FROM task_registry AS t
        WHERE t.update_time <![CDATA[ > ]]> DATE_ADD(#{nowTime},INTERVAL -#{timeout} SECOND)
    </select>

    <update id="registryUpdate" >
        UPDATE task_registry
        SET `update_time` = #{updateTime}
        WHERE `registry_group` = #{registryGroup}
          AND `registry_key` = #{registryKey}
          AND `registry_value` = #{registryValue}
    </update>

    <insert id="registrySave" >
        INSERT INTO task_registry( `registry_group` , `registry_key` , `registry_value`, `update_time`)
        VALUES( #{registryGroup}  , #{registryKey} , #{registryValue}, #{updateTime})
    </insert>

    <delete id="registryDelete" >
        DELETE FROM task_registry
        WHERE registry_group = #{registryGroup}
          AND registry_key = #{registryKey}
          AND registry_value = #{registryValue}
    </delete>

</mapper>
