# 参数属性配置文件
# 定义常用的参数属性，用于自动补全功能
# 多个属性用逗号分隔

# 默认属性列表
parameter.attributes=result,value,list,data,output,response,status,code,message,count,total,items,records

# 属性说明（可选，用于提示）
parameter.attributes.result=执行结果
parameter.attributes.value=值
parameter.attributes.list=列表
parameter.attributes.data=数据
parameter.attributes.output=输出
parameter.attributes.response=响应
parameter.attributes.status=状态
parameter.attributes.code=代码
parameter.attributes.message=消息
parameter.attributes.count=数量
parameter.attributes.total=总数
parameter.attributes.items=项
parameter.attributes.records=记录

