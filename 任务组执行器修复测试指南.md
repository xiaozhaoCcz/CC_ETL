# 任务组执行器修复测试指南

## 修复内容总结

本次修复解决了任务组执行器只执行第一个任务的BUG，主要修改了以下文件：

1. **Async.java** - 修复线程中断问题，增强日志输出
2. **JobGroupExecutorComplete.java** - 增强依赖关系构建日志

## 快速测试步骤

### 1. 重启执行器服务

```bash
# 进入executor-compose目录
cd cc-job/cc-job-executor-compose

# 重新编译（如果需要）
mvn clean package -DskipTests

# 重启服务
# 方式1：使用IDE重启
# 方式2：使用命令行
java -jar target/cc-job-executor-compose-*.jar
```

### 2. 创建测试任务组

#### 方案A：使用现有任务组

如果您已经有包含依赖关系的任务组，直接执行即可。

#### 方案B：创建新的测试任务组

1. **创建3个独立任务**：
   - 任务A：`demoJobHandler1`
   - 任务B：`demoJobHandler2`
   - 任务C：`demoJobHandler3`

2. **创建任务组**：
   - 在 Admin 界面创建任务组
   - 添加节点A、B、C
   - 设置依赖关系：A -> B -> C

3. **保存任务组**

### 3. 执行任务组

1. 在 Admin 界面点击"执行"按钮
2. 观察日志输出

### 4. 验证执行结果

#### ✅ 成功的标志

在日志中应该看到以下输出：

```
[Async] 开始任务调度，总任务数: 3, 初始可执行任务数: 1

# 任务A执行
[Async] 提交任务: <A的ID> 到线程池执行
[Async] ========== 开始执行任务: <A的ID> ==========
demoJobHandler1 beat at:1
...
demoJobHandler1 end
[Async] ========== 任务: <A的ID> 执行完成 ==========
[Async] 后续任务: <B的ID> 入度变为0，加入执行队列，结果: 成功

# 任务B执行
[Async] 提交任务: <B的ID> 到线程池执行
[Async] ========== 开始执行任务: <B的ID> ==========
demoJobHandler2 beat at:1
...
demoJobHandler2 end
[Async] ========== 任务: <B的ID> 执行完成 ==========
[Async] 后续任务: <C的ID> 入度变为0，加入执行队列，结果: 成功

# 任务C执行
[Async] 提交任务: <C的ID> 到线程池执行
[Async] ========== 开始执行任务: <C的ID> ==========
demoJobHandler3 beat at:1
...
demoJobHandler3 end
[Async] ========== 任务: <C的ID> 执行完成 ==========

[Async] 任务组调度正常完成
[JobGroupExecutor] ========== 任务组执行完成 ==========
```

#### ❌ 失败的标志

如果仍然只执行了第一个任务，日志会是这样：

```
[Async] 开始任务调度，总任务数: 3, 初始可执行任务数: 1
[Async] 提交任务: <A的ID> 到线程池执行
demoJobHandler1 beat at:1
...
demoJobHandler1 end
# 没有后续任务的日志
# 直接结束
```

## 详细日志分析

### 1. 依赖关系构建日志

```
[JobGroupExecutor] ========== 开始构建依赖关系 ==========
[JobGroupExecutor] 节点数: 3, 边数: 2
[JobGroupExecutor] 边: 101 -> 102
[JobGroupExecutor] 边: 102 -> 103
[JobGroupExecutor] 节点: 101 的后续节点: [102]
[JobGroupExecutor] 节点: 102 的后续节点: [103]
[JobGroupExecutor] 节点: 101 找到 1 个后续WorkerWrapper
[JobGroupExecutor] 设置依赖: 101 -> 102
[JobGroupExecutor] 节点: 102 找到 1 个后续WorkerWrapper
[JobGroupExecutor] 设置依赖: 102 -> 103
[JobGroupExecutor] 节点: 103 没有后续节点（可能是终点）
[JobGroupExecutor] ========== 依赖关系验证 ==========
[JobGroupExecutor] WorkerWrapper: 101, 依赖数: 0, 后续数: 1
[JobGroupExecutor] WorkerWrapper: 102, 依赖数: 1, 后续数: 1
[JobGroupExecutor] WorkerWrapper: 103, 依赖数: 1, 后续数: 0
[JobGroupExecutor] ========== 依赖关系构建完成 ==========
```

**检查要点**：
- 边数是否正确（应该等于依赖关系的数量）
- 每个节点的后续节点是否正确
- 每个 WorkerWrapper 的依赖数和后续数是否正确

### 2. 任务调度日志

```
[Async] 开始任务调度，总任务数: 3, 初始可执行任务数: 1
[Async] 初始入度为0的任务: 101
```

**检查要点**：
- 总任务数应该等于节点数
- 初始可执行任务数应该 >= 1（至少有一个入度为0的任务）

### 3. 任务执行和入度更新日志

```
[Async] ========== 任务: 101 执行完成 ==========
[Async] 任务: 101 完成，剩余任务数: 2
[Async] 任务: 101 有 1 个后续任务
[Async] 准备更新后续任务: 102 的入度，当前入度: 1
[Async] 后续任务: 102 入度从 1 减少到 0
[Async] 后续任务: 102 入度变为0，加入执行队列，结果: 成功
```

**检查要点**：
- 每个任务完成后，剩余任务数应该减1
- 后续任务的入度应该正确减少
- 当入度变为0时，应该成功加入执行队列

## 常见问题排查

### 问题1：只执行了第一个任务

**可能原因**：
1. 数据库中没有边数据
2. 依赖关系构建失败
3. 后续任务加入队列失败

**排查步骤**：
1. 检查数据库中的 `job_edge` 表：
   ```sql
   SELECT * FROM job_edge WHERE job_id = <任务组ID>;
   ```
   - 如果结果为空，说明没有保存边数据

2. 检查依赖关系构建日志：
   ```
   [JobGroupExecutor] 边数: X
   ```
   - 如果边数为0，说明没有获取到边数据

3. 检查后续任务加入队列的日志：
   ```
   [Async] 后续任务: X 入度变为0，加入执行队列，结果: 失败
   ```
   - 如果结果为"失败"，说明队列操作有问题

### 问题2：任务执行顺序错误

**可能原因**：
依赖关系配置错误

**排查步骤**：
1. 检查依赖关系验证日志：
   ```
   [JobGroupExecutor] WorkerWrapper: X, 依赖数: Y, 后续数: Z
   ```
   - 确认每个任务的依赖数和后续数是否符合预期

2. 检查边的方向：
   ```
   [JobGroupExecutor] 边: X -> Y
   ```
   - 确认边的方向是否正确（from -> to）

### 问题3：任务组提前结束

**可能原因**：
1. 任务执行失败导致异常
2. 超时设置过短
3. 线程被意外中断

**排查步骤**：
1. 检查是否有任务执行失败：
   ```
   [Async] 任务: X 执行失败: ...
   ```

2. 检查是否有超时日志：
   ```
   [Async] 任务组调度超时
   [Async] 等待超时，队列一直为空
   ```

3. 检查是否有中断日志：
   ```
   [Async] 任务调度线程被中断，剩余任务数: X
   [Async] 中断调度线程
   ```

## 性能测试

### 测试场景1：简单链式依赖

**任务组结构**：
```
A -> B -> C
```

**预期时间**：
- 如果每个任务执行10秒，总时间约30秒

### 测试场景2：并行依赖

**任务组结构**：
```
    B
   ↗ ↘
A       D
   ↘ ↗
    C
```

**预期时间**：
- 如果每个任务执行10秒，总时间约30秒（B和C并行）

### 测试场景3：复杂依赖网络

**任务组结构**：
```
    B → D
   ↗     ↘
A         F
   ↘     ↗
    C → E
```

**预期时间**：
- 根据依赖关系，某些任务可以并行执行
- 总时间应该小于串行执行的时间

## 回归测试

确保修复没有影响其他功能：

1. **单个任务执行** - 应该正常工作
2. **无依赖的任务组** - 所有任务应该并行执行
3. **有依赖的任务组** - 按照依赖顺序执行
4. **任务失败处理** - 失败任务应该正确处理（重试或停止）
5. **任务暂停恢复** - 暂停和恢复功能应该正常工作

## 验证清单

- [ ] 任务组中的所有任务都被执行
- [ ] 任务按照依赖关系顺序执行
- [ ] 可以并行的任务确实并行执行
- [ ] 日志输出完整清晰
- [ ] 没有出现异常中断
- [ ] 任务失败时正确处理
- [ ] 暂停恢复功能正常
- [ ] 性能没有明显下降

## 反馈

如果测试过程中发现任何问题，请提供以下信息：

1. **完整的日志输出**（从任务组开始到结束）
2. **任务组的结构**（节点和边的信息）
3. **预期行为和实际行为**
4. **数据库中的相关数据**（job_info, job_node, job_edge）

---

**测试时间**：2025-11-30
**版本**：v1.0
